import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App.js'; //u can omit file extenstions for javascript files

ReactDOM.render(
  <App />, //JSX
  document.getElementById('root')
);
